[project]
name = "wordapp-curate"
version = "0.1.0"
description = "Dataset curation for wordapp"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
