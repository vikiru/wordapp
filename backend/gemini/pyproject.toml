[project]
name = "wordapp-gemini"
version = "0.1.0"
description = "Gemini integration for wordapp"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
