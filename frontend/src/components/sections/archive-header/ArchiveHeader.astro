---
import { Badge } from '@/components/starwind/badge';

interface Props {
	dayCount: number;
	wordCount: number;
}

const { dayCount, wordCount } = Astro.props;
---
<div class="flex flex-col gap-4 pt-4">
		<h1 class="text-h1">Archive</h1>
</div>
